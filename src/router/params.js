/**
 * Objeto que armazena os parâmetros extraídos da URL.
 *
 * O `params` é um objeto vazio que será preenchido com os parâmetros
 * da URL extraídos a partir do caminho definido nas rotas. Os parâmetros
 * são identificados na URL pela notação `:paramName` e são armazenados
 * neste objeto com seus respectivos valores.
 */
const params = {};

export default params;
